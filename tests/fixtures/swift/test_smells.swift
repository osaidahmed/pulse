func test_addition() {
    let result = 2 + 2
    assert(result == 4)
    assert(result > 0)
    assert(result < 10)
}

func test_subtraction() {
    let result = 5 - 3
    assert(result == 2)
}

func normalFunction() -> Int {
    return 42
}
